# delta

Tracked environment-specific changes applied into `src/`.

- `post-create/`: applied after `pei-docker-cli create` and before `pei-docker-cli configure`
- `post-configure/`: applied after `pei-docker-cli configure` (for manual compose edits, etc.)

