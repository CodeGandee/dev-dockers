# External Dependencies

## Structure
- **tracked/**: Git submodules pinned via `.gitmodules`.
- **orphan/**: Local clones or checkouts that are not tracked by the main repository.

## Usage
Avoid editing code inside `extern/tracked/` unless you intend to fork or upstream changes.
